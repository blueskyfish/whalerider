/**
 * The application about information
 */
export interface AppAbout {
  name: string;
  title: string;
  version: string;
  description: string;
  author: string;
  commit: string;
  commitDate: string;
  branch: string;
  buildDate: string;
}
